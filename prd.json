{
  "project": "solar-host",
  "branchName": "feature/S-034",
  "description": "S-034: Resource reservation API — host-local reservation primitives (POST/DELETE /resources/reservations, GET /resources) with in-memory ledger, per-job actual usage polling, and effective availability surfaced via REST and host_health for Solar Control.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Create feature branch",
      "description": "As a developer, I need a dedicated branch for S-034 so reservation work stays isolated from master.",
      "acceptanceCriteria": [
        "Branch feature/S-034 created from master in the solar-host repo",
        "No unrelated changes on the branch"
      ],
      "priority": 1,
      "passes": false,
      "notes": "Supervisor handles git commit/push per workflow; agent creates branch only."
    },
    {
      "id": "US-002",
      "title": "Add resource reservation Pydantic models",
      "description": "As Solar Control, I need typed request/response models for host-local resource reservations across VRAM, RAM, and disk.",
      "acceptanceCriteria": [
        "Add solar_host/resources/models.py with WorkloadType enum (training, inference, other)",
        "ReservationRequest includes job_id, optional requester, workload_type, vram_gb, ram_gb, optional disk_gb, and optional ttl_seconds or expires_at (mutually exclusive)",
        "Reservation runtime model includes id (res-<uuid>), created_at, resolved expires_at, and cached actual_vram_gb, actual_ram_gb, actual_disk_gb, usage_polled_at",
        "ResourceDimensionSnapshot includes total_gb, system_used_gb, reserved_headroom_gb, reported_used_gb, available_gb",
        "ResourceSnapshot includes memory_type, per-dimension vram/ram/disk snapshots, and reservations list of ReservationView",
        "ReservationView includes id, job_id, workload_type, status (pending|running), requested and actual per dimension, expires_at",
        "Export public types from solar_host/resources/__init__.py"
      ],
      "priority": 2,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Collect per-job actual resource usage",
      "description": "As the resource manager, I need accurate per-job actual VRAM, RAM, and disk usage so availability math does not double-count running workloads.",
      "acceptanceCriteria": [
        "Extend memory_monitor.py with get_gpu_process_memory() returning dict[pid -> bytes] via pynvml compute/graphics running processes",
        "Add DockerService.container_stats(container_id) wrapping container.stats(stream=False)",
        "Add solar_host/resources/usage.py that resolves container PID tree (inspect State.Pid + psutil children) and sums VRAM from pynvml and RAM from memory_stats.usage (minus cache when present)",
        "For disk, measure JOBS_DIR/<job_id> workspace directory size via recursive walk; pending reservations use actual=None",
        "When pynvml is unavailable, VRAM actual is None without failing the host",
        "Blocking Docker/pynvml/psutil calls are suitable for asyncio.to_thread from async callers"
      ],
      "priority": 3,
      "passes": false,
      "notes": "VRAM via pynvml per-process accounting; container.stats() is RAM/CPU only."
    },
    {
      "id": "US-004",
      "title": "Implement ResourceManager ledger and availability accounting",
      "description": "As Solar Control, I need the host to track reservations in memory and compute effective availability without overcommitting VRAM, RAM, or disk.",
      "acceptanceCriteria": [
        "Add solar_host/resources/manager.py with thread-safe in-memory store keyed by reservation id",
        "create(req) resolves expires_at, validates capacity, and raises CapacityExceededError when reported_used + requested > total for any dimension",
        "Per dimension: reported_usage = system.usage + sum(max(reserved - actual ?? 0, 0)); available = total - reported_usage",
        "Running vs pending derived from JobStore: running iff job_store.get(job_id).status == running",
        "release(id) returns 404 semantics for unknown id and rejects release when linked job is running (409 semantics via exception)",
        "snapshot() returns ResourceSnapshot with live system usage from memory_monitor and get_disk_info",
        "refresh_usage() updates cached actuals for running reservations using active container from JobExecutor and usage.py plus workspace disk size",
        "cleanup_expired(now) removes expired non-running reservations; running reservations never expire via TTL",
        "Reservations are in-memory only (no disk persistence)"
      ],
      "priority": 4,
      "passes": false,
      "notes": "Capacity rejection uses 409; running release uses 409."
    },
    {
      "id": "US-005",
      "title": "Expose resource reservation REST endpoints",
      "description": "As Solar Control, I need HTTP endpoints to create, release, and query host-local resource reservations.",
      "acceptanceCriteria": [
        "Add solar_host/routes/resources.py with APIRouter and X-API-Key auth inherited from main middleware",
        "POST /resources/reservations returns 201 Created with ReservationView on success",
        "POST returns 422 for invalid body (including both ttl_seconds and expires_at)",
        "POST returns 409 with {error: capacity_exceeded, dimension, requested_gb, available_gb} when capacity exceeded",
        "POST returns 503 when resource manager is unavailable",
        "GET /resources returns 200 ResourceSnapshot including full per-reservation list with per-job actual usage",
        "DELETE /resources/reservations/{id} returns 200 {detail: released, id} on success",
        "DELETE returns 404 for unknown reservation id",
        "DELETE returns 409 when linked job is currently running",
        "Central error-to-HTTP mapping helper mirrors routes/jobs.py patterns"
      ],
      "priority": 5,
      "passes": false,
      "notes": "Intended caller: Solar Control only; SuperNova must not call directly."
    },
    {
      "id": "US-006",
      "title": "Wire ResourceManager and background loops in main.py",
      "description": "As the host process, I need the reservation manager started at boot with periodic usage refresh and expiry cleanup.",
      "acceptanceCriteria": [
        "Instantiate ResourceManager(job_store, docker_service, job_executor) in lifespan and store on app.state.resource_manager",
        "Start resource_usage_poll_loop (~10s) calling refresh_usage()",
        "Start reservation_cleanup_loop (~60s) calling cleanup_expired(now)",
        "Cancel both loops on shutdown alongside existing health and job cleanup tasks",
        "Register resources router via app.include_router(resources.router)",
        "Host remains usable when Docker is unavailable (snapshots work; running-job actuals may stay None)"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "Extend host_health with reservation totals",
      "description": "As Solar Control, I need reservation availability in the host_health Socket.IO payload without fanning out to each host.",
      "acceptanceCriteria": [
        "Extend ws_client.send_health health_data with a reservations block",
        "Block includes per-dimension total, system_used, reserved_headroom, reported_used, available",
        "Block includes active reservation count only (no per-reservation list in the event)",
        "Existing memory and disk fields remain unchanged for backward compatibility"
      ],
      "priority": 7,
      "passes": false,
      "notes": "Per-reservation enumeration stays on GET /resources only."
    },
    {
      "id": "US-008",
      "title": "Add resource reservation test suite",
      "description": "As a developer, I need automated tests covering reservation accounting, API behavior, and health payload changes.",
      "acceptanceCriteria": [
        "Add host/tests/test_resources_*.py covering manager capacity math (pending, running, no double-count, multi-reservation, per-dimension independence)",
        "Tests cover POST 201, capacity 409, invalid 422, manager unavailable 503",
        "Tests cover GET /resources shape including reservation views and JobStore-derived status",
        "Tests cover DELETE 200 pending release, 404 unknown, 409 running job",
        "Tests cover TTL cleanup of expired non-running reservations and retention of running reservations",
        "Tests cover usage.py VRAM/RAM/disk collection with mocked DockerService and pynvml",
        "Tests cover send_health reservations block without reservation list",
        "Tests use TestClient and mock patterns consistent with test_routes_jobs.py and test_job_executor.py",
        "One API-key auth smoke test inherited from middleware"
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Run lint and test verification",
      "description": "As a developer, I need the implementation to pass formatting, lint, and the full test suite before review.",
      "acceptanceCriteria": [
        "uv run ruff check . passes",
        "uv run ruff format --check . passes (black config, line-length 88)",
        "uv run pytest -q passes with no regressions in existing job or memory tests"
      ],
      "priority": 9,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-010",
      "title": "Document API and draft pull request",
      "description": "As a supervisor, I need README documentation and a PR title/body draft to review and merge S-034.",
      "acceptanceCriteria": [
        "Append Resource Reservations (S-034) section to host/README.md documenting POST/DELETE/GET endpoints, request/response shapes, accounting formula, and example curl",
        "Document that API is intended for Solar Control only",
        "Provide PR title: feat(api): add resource reservation API (S-034)",
        "Provide PR body per AGENTS.md with summary, changes list, and Closes #S-034"
      ],
      "priority": 10,
      "passes": false,
      "notes": "Agent does not commit or push; supervisor creates the PR."
    }
  ]
}
